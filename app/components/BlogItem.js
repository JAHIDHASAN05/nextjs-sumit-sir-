import { Blogs } from "@/app/contents/data/blogs/blogs";


const BlogItem = ({params, title}) => {
   
    return (
        <div>
            {title}
        </div>
    );
};

export default BlogItem;